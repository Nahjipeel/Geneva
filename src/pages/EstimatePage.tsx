import { Card, CardHeader, Text } from '@fluentui/react-components';
import { ClockRegular, LocationRegular } from '@fluentui/react-icons';
import { EstimateForm } from '../components/EstimateForm';
import { shop } from '../content/siteContent';

export function EstimatePage() { return <main className="page-shell"><section className="hero hero-compact"><div className="hero-glow" /><div className="eyebrow">● Start with the details</div><h1>Let's get your vehicle back on the road.</h1><p>Tell us what happened and our Batavia team will review the repair, paint, and parts needed for a clear next step.</p></section><div className="section-heading"><div><Text size={700} weight="semibold">Estimate details</Text><p>We'll reply during shop hours.</p></div></div><div className="estimate-layout"><EstimateForm /><aside className="aside-stack"><Card><CardHeader image={<LocationRegular />} header={<Text weight="semibold">Find the shop</Text>} /><p>{shop.address}</p><a href="https://maps.google.com/?q=1211+Lyon+Rd+Batavia+IL+60510">Get directions</a></Card><Card><CardHeader image={<ClockRegular />} header={<Text weight="semibold">Shop hours</Text>} /><p>{shop.hours}</p></Card><Card><CardHeader header={<Text weight="semibold">Estimate status</Text>} /><div className="status-placeholder">Preparing your request</div><Text className="muted">Your estimate status will appear here after submission.</Text></Card></aside></div></main>; }
